const AdminHome = () => {
  return (
    <div>
      <h2>Admin Panel</h2>
      <p>
        You are logged in as an Admin.
      </p>

      <p>
        Admin operations (user creation, approvals) are currently
        handled via backend APIs / Postman.
      </p>

      <p>
        UI dashboards for Admin will be added in a later phase.
      </p>
    </div>
  );
};

export default AdminHome;
